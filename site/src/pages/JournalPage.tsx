import Layout from "@/components/Layout/Layout";
import { PAGES } from "@/consts/pages";

export default function JournalPage() {
  return (
    <Layout currentPage={PAGES.JOURNAL}>
      <div className="container">j</div>
    </Layout>
  );
}